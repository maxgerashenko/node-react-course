{
  "name": "node-starter",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "npx": "^10.2.2"
  },
  "devDependencies": {
    "sass": "^1.62.0"
  }
}
